[pytest]
# Raiz do repo no path pra os testes importarem `src...` e `app` do mesmo jeito que o
# resto do projeto (imports absolutos a partir da raiz, não pacote instalado).
pythonpath = .
testpaths = tests
